# Pronity Backend API configuration
# Copy these values to your .env.local file

# When running locally with docker-compose
NEXT_PUBLIC_PRONITY_API_URL=http://localhost:8000

# When running locally without docker-compose (direct connection to the backend)
# NEXT_PUBLIC_PRONITY_API_URL=http://localhost:8000
