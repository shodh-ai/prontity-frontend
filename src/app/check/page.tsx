"use client"; // 👈 Required for client-side only code like event handlers, local state

import React from "react";
import MainLayout from '@/components/layout/MainLayout';

export default function DashRoxPage() {
  return (
    <MainLayout>
      <h1>Check</h1>
    </MainLayout>
  );
}
